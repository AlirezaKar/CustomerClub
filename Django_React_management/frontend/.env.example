# =============================================================================
# Vite — copy to .env.local or .env (never commit real secrets)
# =============================================================================
# Backend API base URL (no trailing slash). Must match where Django serves /api.
VITE_API_URL=http://localhost:8000/api
